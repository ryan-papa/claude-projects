# Claude 리뷰 r1 — harness-ignore-expand (간소 PRD)

| # | 항목 | 점수 | 근거 |
|---|------|:---:|------|
| 1 | 변경 이유 명확성 | 9 | 토큰 소모 문제 진단 + 실제 레포(`museum-finder`, `naver-cafe-monitor`) 발견 패턴·11M `spike/` 같은 정량 근거 제시 |
| 2 | 영향 파일 정확성 | 9 | `.claudeignore` 추가 섹션 4종·`.codexignore` 신규 명시. 실제 파일 변경(추가 only, 기존 라인 보존)과 일치 |
| 3 | 롤백 계획 | 9 | `git revert <commit>` 단발 + 부수효과(토큰 절감 소실뿐) 명시. 스킬·CI 미참조 확인 |
| 4 | 검증 방법 구체성 | 9 | `git check-ignore -v` 샘플 경로 4건, `diff` 0 라인, 의도 외 무력화 점검 명령까지 제시 |
| 5 | 변경 안전성 | 8 | 추가 only, `src/`·`docs/`·`*.md` 무시 패턴 없음 확인. 단, `*.enc` + `.env.enc` 중복(이미 `*.enc`가 포함)은 무해하나 잉여. `vendor/`는 Go·Ruby 양쪽 등장(기존부터 존재, 회귀 아님) |
| 6 | .codexignore 미러 정합성 | 10 | 헤더(1~5행) 제외 본문 6행 이후 `diff` 0 라인 검증 완료 |

**평균: 9.0 / 10**

## 판정
**통과** (평균 9.0, 모든 항목 8.0 이상)

## High/Critical 지적
없음.

## Minor (반영 권장 아님, 참고)
- `*.enc` 패턴이 이미 `.env.enc`를 포함하므로 후자는 잉여 라인. 가독성 목적이면 유지 가능.
- `tmp/`·`.tmp/`는 일부 프로젝트가 의미 있게 사용할 가능성 있으니 향후 회귀 시 우선 의심 후보.
