# Claude Meta Review r1 — audit-B-high

리뷰어: 서브에이전트(general-purpose, Opus 4.7 1M)
대상: `docs/prd/audit-B-high/prd.md`
일시: 2026-05-01

## 평가 (각 10점, 임계 ≥7, 평균 ≥8.0)

| # | 항목 | 점수 | 근거 |
|---|------|------|------|
| 1 | 변경 이유 명확성 (H1~H5 매핑) | 9 | H1~H5 표 형태로 1:1 매핑. 현 상태 수치(186/188줄, 4곳 중복) 실측치와 일치 (CLAUDE.md=186, harness-prd.md=188 확인). H5 산문 1줄 위치도 실제 `harness-codex-review.md:69` 헤더 인근 산문과 정합. 단, H1 "5책임/3책임 혼재"의 "책임" 정의가 본문에 명시 부족 — 분리 기준의 추적성이 1점만큼 약함 |
| 2 | 영향 파일 정확성 | 8 | 신설 1개(`harness-absolute-rules.md`)는 SSOT 단일화 명분으로 정당. CLAUDE.md L130~150 절대 규칙 섹션이 실제로 L135~171 범위에 존재 — 줄번호 표기가 살짝 어긋나지만 위치 식별은 가능. `harness-prd.md` PR-C 이관은 합리적 범위 분할. `harness-ship.md` 도 영향 파일에 포함되어 4곳 중복 모두 커버. 누락 의심: `AGENTS.md` 가 절대 규칙 일부를 인용하는지 미검증 (PRD에 언급 없음, 1점 감점) |
| 3 | 롤백 안전성 | 9 | 통합 브랜치 + revert 1회 + main 미도달 보장. 신설 파일 삭제도 revert 자동 처리. 단, 4개 리뷰 스킬의 링크 변경분이 revert 후 외부 참조(예: 사용자 메모리)와의 dangling 가능성에 대한 한 줄 언급이 없어 만점 보류 |
| 4 | 검증 방법 구체성 | 8 | 실제 grep 명령 3종 모두 동작 확인. `grep -v 'absolute-rules'` 자가 제외도 정확. `wc -l` 대상 파일 모두 존재. 다만 "죽은 링크 0건" 검증 명령(예: markdown-link-check)이 검증 명령 블록에 빠져 있어 수동 의존 → 1점 감점. "메타 리뷰 게이트 r1·r2" 표현은 메타 단일 리뷰 정책(r1)과 약간 충돌 — 본문 문구상 r2까지 요구한 의도라면 절대 규칙 SSOT 위반 위험 (CLAUDE.md L141 "{meta} = 간소 PRD 단일 리뷰 전용") |
| 5 | 기존 규칙과의 정합성 (CLAUDE.md 진입점 유지) | 8 | 절대 규칙을 별도 파일로 분리해도 CLAUDE.md "Harness Engineering" 섹션이 1줄 링크로 진입점 유지 가능. 200줄 이하 원칙·SSOT 원칙·하네스 동기화 원칙 모두 부합. 단, 절대 규칙은 "사용자가 CLAUDE.md만 읽어도 즉시 인식"되어야 하는 강한 게이트 성격 — 클릭 1회로 분리될 경우 신규 사용자 온보딩 충격 1단계 증가. 현 PRD는 "1줄 링크" 외에 핵심 규칙 5~7개 한 줄 요약(불릿)을 CLAUDE.md에 잔류시키는 절충안 미제시 → 1점 감점 |
| 6 | 범위 적절성 (메타 단축 경로 한계) | 9 | 단축 경로 4섹션(이유·영향·롤백·검증) 충실. 신설 1개·기존 9개 파일 수정으로 적정 규모. PR-C 분리(harness-prd.md 분할)로 야심 억제도 명시. `rp-init`·`rp-task`·`rp-dev` 스킵 조건도 단축 경로 정의 부합. 다만 9개 파일 동시 수정은 메타 PR 평균 대비 상단 — 1점만 보류 |

**평균:** (9+8+9+8+8+9) / 6 = **8.50**
**최저:** 8 (≥7 충족)

## 판정: PASS

평균 8.50 ≥ 8.0, 최저 8 ≥ 7. 다음 단계(Codex 메타 리뷰) 진입 가능.

## 핵심 지적 (반영 권고)

| 우선순위 | 지적 | 권고 |
|----------|------|------|
| High | "메타 리뷰 게이트 Claude r1·r2" 표현이 메타 단일 리뷰 정책과 충돌 가능 | 검증 표 항목을 "Claude r1 + Codex 1회 모두 통과"로 정정 (r2는 미달 시에만) |
| Medium | CLAUDE.md 진입점 약화 우려 | 절대 규칙 파일 분리 시, CLAUDE.md 본문에 핵심 5~7개 규칙명만 불릿으로 잔류시키고 상세는 링크로 위임 |
| Medium | `AGENTS.md` 영향 미검증 | Codex 어댑터가 절대 규칙 본문을 직접 인용하는지 1회 grep 후 영향 파일 표 보강 (영향 없으면 그대로) |
| Low | "죽은 링크 0건" 검증 자동화 | `grep -rn '\](.*\.md)' docs/ CLAUDE.md` 또는 markdown-link-check 명령을 검증 블록에 추가 |
| Low | H1 "책임" 정의 모호 | 분리 기준(예: "1파일 = 1주제 = 1독자")을 1줄 명시 |

## 다음 액션

1. (선택) High 지적 1건 반영 후 PRD 패치
2. 메인 에이전트가 `/codex:review --wait` 1회 실행 → `review-codex-meta.md` 저장
3. 두 리뷰 모두 통과 시 `rp-ship`(메타 분기, `--base main`) 진입
